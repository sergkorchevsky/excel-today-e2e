{
  "name": "excel-today-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:pw": "playwright install --with-deps",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "demo": "PWVIDEO=1 playwright test --headed",
    "format": "prettier -w ."
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "typescript": "^5.5.4",
    "prettier": "^3.3.3"
  }
}
